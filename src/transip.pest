jaja = { SOI ~ vps_command ~ EOI }
name = { ASCII_ALPHANUMERIC+ }
space = _{ " " }
vps_list = { "list" ~ space* }
vps_item = { "item" ~ space+ ~ name ~ space* }
vps_reset = { "reset" ~ space+ ~ name ~ space* }
vps_command = { "vps" ~ space+ ~ (vps_list | vps_item | vps_reset ) }
